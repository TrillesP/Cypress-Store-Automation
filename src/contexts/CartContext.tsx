import { createContext, ReactNode, useReducer } from 'react'
import {
  ActionTypes,
  Cart,
  CartItem,
  Product,
  cartReducer,
} from '../reducers/cart'

// interface CartContextType {
//   cart: Cart,

// }

// export function CartContext = createContext({} as )
